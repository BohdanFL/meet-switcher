{
  "name": "meet-switcher",
  "version": "1.0.0",
  "description": "Quick 1-click & hotkey switching between student screen shares in Google Meet",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/detector.test.ts tests/logger.test.ts tests/call-monitor.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.308",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
